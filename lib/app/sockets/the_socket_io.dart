import 'package:get/get_connect/connect.dart';

class TheSocketIO extends GetConnect {
  // Future<void> sendMessage(String message) async {
  //   // Send a message to the Socket.IO server
  //   socket.emit('message', message);
  //
  //   // Make an HTTP POST request to the API endpoint
  //   final response = await post(
  //     'http://localhost:3000/api/send-message',
  //     {'message': message},
  //   );
  //
  //   // handle the response
  //   if (response.hasError) {
  //     throw Exception('Failed to send message: ${response.statusText}');
  //   }
  // }
  //
  // Future<void> fetchMessage() async {
  //   // Send a message to the Socket.IO server
  //   socket.emit('fetch_message', 'fetch_message');
  //
  //   // Make an HTTP POST request to the API endpoint
  //   final response = await post(
  //     'http://localhost:3000/api/fetch-message',
  //     {'fetch_message': 'fetch_message'},
  //   );
  //
  //   // handle the response
  //   if (response.hasError) {
  //     throw Exception('Failed to send message: ${response.statusText}');
  //   }
  // }
  //
  // Future<void> stopTyping() async {
  //   // Send a message to the Socket.IO server
  //   socket.emit('stop_typing', 'stop_typing');
  //
  //   // Make an HTTP POST request to the API endpoint
  //   final response = await post(
  //     'http://localhost:3000/api/stop-typing',
  //     {'stop_typing': 'stop_typing'},
  //   );
  //
  //   // handle the response
  //   if (response.hasError) {
  //     throw Exception('Failed to send message: ${response.statusText}');
  //   }
  // }
  //
  // Future<void> typing() async {
  //   // Send a message to the Socket.IO server
  //   socket.emit('typing', 'typing');
  //
  //   // Make an HTTP POST request to the API endpoint
  //   final response = await post(
  //     'http://localhost:3000/api/typing',
  //     {'typing': 'typing'},
  //   );
  //
  //   // handle the response
  //   if (response.hasError) {
  //     throw Exception('Failed to send message: ${response.statusText}');
  //   }
  // }
  //
  // Future<void> uploadFile(String file) async {
  //   // Send a message to the Socket.IO server
  //   socket.emit('upload_file', file);
  //
  //   // Make an HTTP POST request to the API endpoint
  //   final response = await post(
  //     'http://localhost:3000/api/upload-file',
  //     {'upload_file': file},
  //   );
  //
  //   // handle the response
  //   if (response.hasError) {
  //     throw Exception('Failed to send message: ${response.statusText}');
  //   }
  // }
  //
  // Future<void> downloadFile(String file) async {
  //   // Send a message to the Socket.IO server
  //   socket.emit('download_file', file);
  //
  //   // Make an HTTP POST request to the API endpoint
  //   final response = await post(
  //     'http://localhost:3000/api/download-file',
  //     {'download_file': file},
  //   );
  //
  //   // handle the response
  //   if (response.hasError) {
  //     throw Exception('Failed to send message: ${response.statusText}');
  //   }
  // }
  //
  // Future<void> deleteFile(String file) async {
  //   // Send a message to the Socket.IO server
  //   socket.emit('delete_file', file);
  //
  //   // Make an HTTP POST request to the API endpoint
  //   final response = await post(
  //     'http://localhost:3000/api/delete-file',
  //     {'delete_file': file},
  //   );
  //
  //   // handle the response
  //   if (response.hasError) {
  //     throw Exception('Failed to send message: ${response.statusText}');
  //   }
  // }
  //
  // Future<void> fetchFile(String file) async {
  //   // Send a message to the Socket.IO server
  //   socket.emit('fetch_file', file);
  //
  //   // Make an HTTP POST request to the API endpoint
  //   final response = await post(
  //     'http://localhost:3000/api/fetch-file',
  //     {'fetch_file': file},
  //   );
  //
  //   // handle the response
  //   if (response.hasError) {
  //     throw Exception('Failed to send message: ${response.statusText}');
  //   }
  // }
  //
  // Future<void> fetchAllFiles() async {
  //   // Send a message to the Socket.IO server
  //   socket.emit('fetch_all_files', 'fetch_all_files');
  //
  //   // Make an HTTP POST request to the API endpoint
  //   final response = await post(
  //     'http://localhost:3000/api/fetch-all-files',
  //     {'fetch_all_files': 'fetch_all_files'},
  //   );
  //
  //   // handle the response
  //   if (response.hasError) {
  //     throw Exception('Failed to send message: ${response.statusText}');
  //   }
  // }
}
